import React from "react";
import ReactDOM from "react-dom/client";
import "@fontsource/sn-pro/index.css";
import { App } from "./App";

ReactDOM.createRoot(document.getElementById("app")!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
);
